{# This is just a layout example for the styleguide.
  Do not use this template directly. #}

{% set styles = [] %}
{% set styles = gap ? styles|merge(['--gap: var(' ~ gap ~ ');']) : styles %}
{% set styles = measure ? styles|merge(['--measure: ' ~ measure ~ ';']) : styles %}

{% set classes = [
  'switcher',
] %}

<div {{ attributes.addClass(classes).setAttribute('style', styles|join) }}>
  {% for i in range(1, num_items) %}
    <div class="box" style="background: black; text-align: center; color: #fff; font-weight: bold;">{{ i }}</div>
  {% endfor %}
</div>
