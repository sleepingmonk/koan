{% set variant_type = variant ? 'button--' ~ variant : 'button--primary' %}
{% set variant_size = size ? 'button--' ~ size : 'button--normal' %}
{% set classes = [
  'button',
  variant_type,
  variant_size
] %}

{{ link(label, href, { 'class': classes }) }}

