{% set variant_class = variant ? 'brow--' ~ variant : 'brow--default' %}
{% set classes = [
  'brow',
  variant_class,
] %}

<div class="{{ classes|join(' ') }}">
  {% if part_one %}
    <span class="brow__accent">{{ part_one }}</span>
  {% endif %}
  {% if part_two %}
    <span class="brow__text">{{ part_two }}</span>
  {% endif %}
</div>
